name: Weekly Schedule Update

on:
  schedule:
    - cron: '0 9 * * 1'  # 毎週月曜日の9時（UTC）
  workflow_dispatch:

jobs:
  update-schedule:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Save Google credentials
        run: |
          mkdir -p creds
          echo "${{ secrets.GOOGLE_CREDENTIALS }}" > creds/service_account.json

      - name: Run script
        env:
          GOOGLE_APPLICATION_CREDENTIALS: creds/service_account.json
        run: |
          python main.py

